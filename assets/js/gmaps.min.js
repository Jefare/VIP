  // 百度地图API功能
  var map = new BMap.Map("gmap");    // 创建Map实例
  map.centerAndZoom(new BMap.Point(113.842841,22.883991), 11);  // 初始化地图,设置中心点坐标和地图级别
  map.enableScrollWheelZoom(true);     //开启鼠标滚轮缩放
  var scaleCtrl = new BMapGL.ScaleControl();  // 添加比例尺控件
  map.addControl(scaleCtrl);
  var zoomCtrl = new BMapGL.ZoomControl();  // 添加比例尺控件
  map.addControl(zoomCtrl);
  var marker = new BMap.Marker(point);        // 创建标注
  map.addOverlay(marker);                     // 将标注添加到地图中

  var infoWindow = new BMap.InfoWindow("<b class='iw_poi_title'>宏远酒店</b><div class='iw_poi_content'>热线：+86-769-2241-8888<br>地址：广东省东莞市南城区宏远路一号<br></div>"); // 创建信息窗口对象
  marker.addEventListener("click", function(){ //给标注添加点击事件
  marker.openInfoWindow(infoWindow);
  });
  marker.openInfoWindow(infoWindow);